<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow network access for proxy functionality -->
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>

    <!-- Allow executing the native binary -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>

    <!-- Disable library validation to allow loading the native binary -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Allow arbitrary loads (needed for the native binary) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow JIT (needed for some operations) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- File access (read/write for settings and logs) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
</dict>
</plist>
