# راهنمای ساده فارسی

<div align="center">
  <strong>راهنمای کاربری Stream Gate Plus</strong><br>
  یک رابط گرافیکی بهینه شده برای اتصال به سرورهای Slipstream Plus
</div>

---

## 👀 تور سریع (نسخه Plus)

- **دانلود و نصب** برنامه از صفحه Release
- **تنظیم سرور** (Domain) و **DNS Resolver** (مثلاً: `8.8.8.8:53`)
- **هسته بهینه شده Slipstream Plus**: استفاده از هسته Rust با سرعت بالا و اختصاصی.
- **DNS Checker (اختیاری)**: اجرا کنید و برای ردیف‌هایی که وضعیت **OK** دارند روی دکمه **Use** بزنید تا DNS Resolver همان مقدار تنظیم شود
- **شروع VPN** با دکمه "Start VPN" و بررسی اینکه وضعیت‌ها "Running" شوند
- **بررسی اتصال** با دکمه "Test Proxy Connection" و مشاهده لاگ‌ها در صورت نیاز
- **اختیاری**: اشتراک اینترنت روی Wi‑Fi برای موبایل از طریق HTTP Proxy داخلی (`8080`)

---

## 📥 نصب و راه‌اندازی

### دانلود و نصب

1. به صفحه [Releases](https://github.com/free-mba/Stream-Gate/releases/latest) بروید
2. دانلود مستقیم آخرین نسخه (v1.0.0):

| پلتفرم | دانلود |
|----------|----------|
| macOS (Apple Silicon) | [Stream-Gate-macOS-ARM64.dmg](https://github.com/free-mba/Stream-Gate/releases/latest/download/Stream-Gate-macOS-ARM64.dmg) |
| macOS (Intel) | [Stream-Gate-macOS-Intel.dmg](https://github.com/free-mba/Stream-Gate/releases/latest/download/Stream-Gate-macOS-Intel.dmg) |
| Windows (64-bit) | [Stream-Gate-Windows-x64.exe](https://github.com/free-mba/Stream-Gate/releases/latest/download/Stream-Gate-Windows-x64.exe) |
| Windows (32-bit) | [Stream-Gate-Windows-x86.exe](https://github.com/free-mba/Stream-Gate/releases/latest/download/Stream-Gate-Windows-x86.exe) |
| Linux (x86_64) AppImage | [Stream-Gate-Linux-x64.AppImage](https://github.com/free-mba/Stream-Gate/releases/latest/download/Stream-Gate-Linux-x64.AppImage) |
| Linux (x86_64) DEB | [Stream-Gate-Linux-x64.deb](https://github.com/free-mba/Stream-Gate/releases/latest/download/Stream-Gate-Linux-x64.deb) |

### راه‌اندازی اولیه

1. برنامه **Stream Gate** را باز کنید
2. (اختیاری) تنظیمات را پیکربندی کنید:
   - **DNS Resolver**: یک DNS عمومی (پیش‌فرض: `8.8.8.8:53`)
   - **Domain**: آدرس دامنه سرور Slipstream Plus شما (مثال: `s.example.com`)
   - **System Proxy**: برای تنظیم خودکار پروکسی سیستم فعال کنید (توصیه می‌شود)

3. روی دکمه **"Start VPN"** کلیک کنید
4. منتظر بمانید تا وضعیت اتصال "Running" نشان داده شود
5. حالا تمام ترافیک اینترنت شما از طریق VPN مسیریابی می‌شود!

---

## 🚀 تنظیمات شبکه پیشرفته (Advanced Networking)

شما می‌توانید تنظیمات اتصال را در صفحه **Settings** شخصی‌سازی کنید:

### کنترل الازدحام (Congestion Control)
- **Auto (پیش‌فرض)**: بهترین الگوریتم را بر اساس حالت DNS انتخاب می‌کند (BBR برای Authoritative و DCubic برای Recursive).
- **BBR+ (بهینه شده)**: الگوریتم اختصاصی نسخه Plus. عالی برای شبکه‌های ناپایدار و ایران.
- **DCubic**: تهاجمی‌تر. مناسب برای اتصال‌های پایدار و سیمی.

### QUIC Keep Alive
- مقدار پیش‌فرض `400` ثانیه است.
- اگر در شبکه‌های موبایل با قطعی مکرر مواجه می‌شوید، این مقدار را کم کنید (مثلاً `30`).

---

## 🖥️ آموزش نصب سرور Slipstream Plus (Plus Server Setup)

برای استفاده از نسخه Plus، شما به یک سرور بهینه شده نیاز دارید.

### راه‌اندازی سرور (سریع)

برای راه‌اندازی سریع سرور Stream Gate Plus، از اسکریپت زیر استفاده کنید:

**نصب با یک دستور:**
```bash
bash <(curl -Ls https://raw.githubusercontent.com/Fox-Fig/slipstream-rust-plus-deploy/master/slipstream-rust-plus-deploy.sh)
```

### پیش‌نیازهای سرور

- یک سرور لینوکس (Fedora, Rocky, CentOS, Debian, یا Ubuntu)
- یک دامنه با دسترسی به تنظیمات DNS
- دسترسی root یا sudo روی سرور

### مراحل بعد از راه‌اندازی سرور

1. رکوردهای DNS را تنظیم کنید (راهنمای کامل در [slipstream-rust-plus-deploy](https://github.com/Fox-Fig/slipstream-rust-plus-deploy))
2. منتظر بمانید تا DNS به‌روزرسانی شود (می‌تواند تا 24 ساعت طول بکشد)
3. در Stream Gate، دامنه سرور خود را وارد کنید (مثلاً: `s.example.com`)
4. DNS Resolver را وارد کنید (مثلاً: `8.8.8.8:53`)
5. روی **"Start VPN"** کلیک کنید تا متصل شوید!

---

## 📱 اشتراک‌گذاری اینترنت کامپیوتر با موبایل (همان شبکه)

اگر کامپیوتر و موبایل شما به یک شبکه Wi-Fi متصل هستند، می‌توانید موبایل خود را طوری تنظیم کنید که از اینترنت کامپیوتر (شامل VPN) استفاده کند.

### پیدا کردن آدرس IP کامپیوتر

**Windows:**
```cmd
ipconfig
```

**macOS/Linux:**
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

### 📱 تنظیمات موبایل

1. به تنظیمات Wi-Fi بروید و روی شبکه متصل شده بزنید.
2. بخش **Proxy** را روی **Manual** تنظیم کنید.
3. در قسمت **Hostname** آدرس IP کامپیوتر را وارد کنید.
4. در قسمت **Port** عدد **8080** را وارد کنید.
5. تنظیمات را ذخیره کنید.

---

## 🎯 استفاده از VPN

### پنل وضعیت
- وضعیت اتصال را به صورت زنده مشاهده کنید.
- نشانگرهای **Client**، **HTTP Proxy** و **System Proxy** باید سبز (Running) باشند.

### توقف VPN
- وقتی می‌خواهید قطع شوید، روی دکمه **"Stop VPN"** کلیک کنید.
- پروکسی سیستم به صورت خودکار غیرفعال می‌شود.

---

<div align="center">
  <strong>ساخته شده با ❤️ برای کسانی که به یاد داریم</strong>
</div>
